## RAG-агент для поиска терапевтических мишеней болезни Альцгеймера

## 📋 Требования

 

### Программные требования

* Python 3.10+

* Ollama

* Учетные данные NCBI (Email и API Key)

 

### Аппаратные требования

* GPU с поддержкой CUDA (от 8 ГБ VRAM). При меньшем объёме скорость ответом будет значительно снижена.

* ОЗУ от 12 ГБ

* Свободное место на диске: от 10 ГБ

\
\

## 🚀 Развёртывание

1. __Клонирование и среда:__

```bash
git clone https://github.com/WeinerGero/biocad-ml-internship.git

python -m venv .venv
source .venv/bin/activate  # для Windows: .venv\Scripts\activate
```

2. __Установка зависимостей:__

```bash
pip install -r requirements.txt
```

3. __Настройка Ollama:__

```bash
ollama pull mistral-nemo            # Установка модели локально 
ollama serve                        # Запуск сервиса
```

4. __Конфигурация:__

Создайте файл .env в корне проекта:

```python
EMAIL="your_email@example.com"
NCBI_API_KEY="your_api_key"
```



## 📖 Инструкция по пользованию

```
BIOCAD-ML-INTERNSHIP/
├── data/                   - Хранилище сырых и обработанных научных статей (JSON).
├── db/                     - Локальная база данных ChromaDB (векторные эмбеддинги и SQLite).
├── notebooks/              - Этап песочницы: эксперименты, EDA и отладка алгоритмов.
├── src/                    - Исходный код системы, разбитый на функциональные модули:
│   ├── db/                 - Логика построения индексов и управления базой знаний.
│   ├── generator/          - Ядро RAG (Multi-Query, Hybrid Search, RRF, LLM-инференс).
│   ├── judge/              - Система автоматизированной оценки качества ответов.
│   ├── parser/             - Инструменты сбора (PubMed API) и препроцессинга текстов.
│   └── ui/                 - Фронтенд на Streamlit (компоненты, конфиги, менеджер сессий).
├── .env                    - Приватные ключи доступа (API PubMed, настройки моделей).
├── app.py                  - Точка входа в веб-интерфейс ассистента.
├── chat_history.json       - Локальное хранилище истории диалогов пользователя.
├── requirements.txt        - Список зависимостей для репликации окружения.
└── README.md               - Техническая документация проекта.
```


1. __Подготовка данных:__

Соберите датасет статей и постройте поисковые индексы:

```bash
python -m src.parser.control_parser  # Сбор и очистка данных
python -m src.db.build_index         # Построение векторного индекса и BM25
```

2. __Запуск приложения:__

```bash
streamlit run app.py
```

3. __Работа с интерфейсом:__

* Введите вопрос в поле чата.

* В боковой панели настройте параметр k (количество анализируемых источников).

* Для детального анализа используйте раскрывающийся список источников под ответом ассистента.



## 🏗️ Архитектура системы

Система реализована как модульный пайплайн:

* __Парсер:__ Сбор данных через Entrez API

* __Поиск:__  Сочетание семантического поиска (модель PubMedBert-MS-MARCO) и вероятностного поиска (BM25). Поискал на Hugging Face модель для чанкинга, оказывается есть эмбединг модель, уже обученная на PubMed данных: [pritamdeka/S-PubMedBert-MS-MARCO](https://huggingface.co/pritamdeka/S-PubMedBert-MS-MARCO)

* __Создание запросов:__ Модуль Multi-Query Expansion генерирует три стратегии поиска (Macro, Micro, Relational) на английском языке для преодоления семантического разрыва.

* __Reciprocal Rank Fusion (RRF):__ Агрегация найденных в векторной БД результатов через Reciprocal Rank Fusion с принудительной группировкой по уникальным PMID 

* __Генерация:__ Используется модель Mistral-Nemo (12B) с системным промптом. 



## 🛠️ Технические проблемы и решения

В процессе разработки были решены следующие задачи:

1. __Проблема низкой полноты поиска на специфических сущностях:__

Векторный поиск игнорировал конкретные генетические варианты (например, rs11786896). Частично решено внедрением гибридного поиска (BM25 + Vector), что позволило обнаруживать статьи по точным вхождениям идентификаторов мутаций.

2. __Проблема семантической избыточности:__ Контекстное окно переполнялось чанками из одной релевантной статьи. Внедрена группировка по PMID: система суммирует веса всех чанков одного источника, но в итоговый контекст подает только уникальные статьи (Top-N уникальных источников).

3. __Проблема аппаратных ограничений (4 ГБ VRAM):__ Локальный инференс 12B модели приводил к высокой задержке. Проблема решена на уровне UI через внедрение асинхронных уведомлений о статусе выполнения каждого этапа (генерация стратегий, поиск, ранжирование), что улучшило UX при длительном ожидании ответа.

4. __Проблема галлюцинаций в терминологии:__ Модель пыталась переводить устоявшиеся термины (например, превращая Astrocytes в "астросклеры"). Решено через жесткий Terminology Lock в системном промпте (инструкция отвечать на русском, но сохранять сущности на английском). Также помогло использование более тяжеловесных моделей.



## 📓 Описание ноутбуков (Notebooks)

Ноутбуки отражают твой исследовательский путь. Рекомендую располагать их в порядке выполнения пайплайна:

1. ```1_parser.ipynb```
    - __Цель:__ Отработка запросов к PubMed API через Biopython.
    - __Содержание:__ Формирование поискового запроса, парсинг XML-структур и извлечение специфических секций.

2. ```2_EDA.ipynb```
    - __Цель:__ Анализ собранного корпуса текстов.
    - __Содержание:__ Статистика по длине статей, распределение публикаций по годам, поиск наиболее частотных терминов и кластеризация тем.

3. ```3_chunking.ipynb```
    - __Цель:__ Разбиение текста.
    - __Содержание:__ Создание БД и чанкинг.

4. ```4_RAG.ipynb```
    - __Цель:__ Сборка и анализ полученных оветов.
    - __Содержание:__ Реализация Hybrid Search и Multi-Query, анализ ответов по набору из 10 вопросов и 5 метрикам: Cosine Similarity, Relevance, Depth, Clarity, Structure.

Результаты RAG на наборе вопросов на русском языке:

```
Cosine Similarity    0.97109 | 1
Relevance            3.50000 | 5
Depth                3.80000 | 5
Clarity              3.40000 | 5
Structure            2.60000 | 5
```



## 📈 Возможности и развитие

1. __Контейнеризация:__ Упаковка в Docker-контейнер, чтобы не деплоить каждый раз руками 

2. __Тестирование:__ Покрытие модульными тестами алгоритма RRF и внедрение интеграционных тестов для оценки Retrieval Hit Rate на контрольной выборке.

3. __Оптимизация поиска:__ Использование Cross-Encoder моделей для переранжирования топ-100 кандидатов, что позволит повысить точность (Precision) без ущерба для полноты (Recall).

4. __Улучшение эмбеддингов:__ Тюнинг модели эмбеддингов на специфическом подмножестве статей по болезни Альцгеймера.


